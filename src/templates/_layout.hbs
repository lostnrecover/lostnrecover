<!DOCTYPE html>
<html>

<head>
	<title>{{ title }} - Lost n Found</title>
	<link rel="stylesheet" href="https://unpkg.com/marx-css/css/marx.min.css" />
	<link rel="stylesheet" href="/public/css/app.css" />
</head>
<body>
	<main>
		<header>
		<h1><a href="/">Lost n Found</a></h1>
		<nav>{{# if session.email }}
			<a href="/account">{{ __ 'Account' }}</a>
			<a href="/logout">{{ __ 'Logout' }}</a>{{/if}}
		</nav>
		</header>
		{{{body}}}
	</main>
	{{ log @root }}
</body>

</html>