<p>{{ __ 'Please click on the magic link provided in the mail.'}}</p>
{{#if email}}
<form action="/login" method="post">
  <input type="hidden" id="email" name="email" value="{{email}}"/>
  <button type="submit">{{ __ 'Resend Magic Link' }}</button>
</form>
{{/if}}
{{!-- <form action="/auth" method="get">
<p>
  <label for="token">{{ __ 'Token' }}</label>
  <textarea name="token" id="token">
  </textarea>
  <button>{{ __ 'Start session' }}</button>
</p>
</form> --}}